import 'dart:math';

final binaryKilo = pow(2, 10);
final binaryMega = pow(2, 20);
final binaryGiga = pow(2, 30);
final binaryTera = pow(2, 40);
final binaryPeta = pow(2, 50);
final binaryExa = pow(2, 60);
final binaryZetta = pow(2, 70);
final binaryYotta = pow(2, 80);

final femto = pow(10, -15);
final pico = pow(10, -12);
final angstrom = pow(10, -10);
final nano = pow(10, -9);
final micro = pow(10, -6);
final milli = pow(10, -3);
final centi = pow(10, -2);
final deci = pow(10, -1);
final deca = pow(10, 1);
final hecto = pow(10, 2);
final kilo = pow(10, 3);
final mega = pow(10, 6);
final giga = pow(10, 9);
final tetra = pow(10, 12);
final peta = pow(10, 15);

// UTF-32 code point
const unicodeMicro = '03bc';
const unicodeAngstrom = '00c5';

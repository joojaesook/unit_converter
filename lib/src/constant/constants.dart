import 'dart:math';

final kiloBinary = pow(2, 10);
final megaBinary = pow(2, 20);
final gigaBinary = pow(2, 30);
final teraBinary = pow(2, 40);
final petaBinary = pow(2, 50);
final exaBinary = pow(2, 60);
final zettaBinary = pow(2, 70);
final yottaBinary = pow(2, 80);
